# Outputs Directory

**Purpose:** Generated artifacts from AIOS expansion packs
**Location:** `/outputs/`
**Status:** Generated content (not source code)

---

## 📁 Structure

This directory contains **generated outputs** created by AIOS expansion packs:

```
outputs/
├── courses/         ← Generated courses (CreatorOS expansion)
│   ├── {slug}/      ← Each course in its own folder
│   │   ├── COURSE-BRIEF.md
│   │   ├── README.md
│   │   ├── curriculum.yaml
│   │   ├── lessons/
│   │   ├── assessments/
│   │   └── resources/
│   └── ...
│
├── minds/           ← Processed minds (MMOS Mind Mapper expansion)
│   ├── {mind-name}/
│   │   ├── synthesis/
│   │   ├── analysis/
│   │   └── sources/
│   └── ...
│
└── database/        ← MMOS database files
    ├── mmos.db      ← Main SQLite database
    └── .gitkeep
```

---

## ⚠️ Important Notes

### Generated Content

**This directory contains generated artifacts, not source code:**

- ✅ **Courses:** Generated by `*generate-course` and `*continue-course` tasks
- ✅ **Minds:** Processed by MMOS Mind Mapper expansion
- ✅ **Stories:** Story execution outputs

### Version Control

**Gitignore policy:**
- Generated content is **not committed** to git (see `.gitignore`)
- Database files (`outputs/database/*`) are **not committed**
- Only directory structure is tracked (`.gitkeep` files)
- Each user generates their own outputs locally

**Note about logs:**
- Execution logs are in `docs/logs/` and **are versioned**
- Logs document process history and are part of documentation

### Manual Editing

**⚠️ Warning:**
- Content here is **generated** - manual edits may be overwritten
- To modify courses: edit the **brief** and regenerate
- To modify minds: edit **source materials** and reprocess

---

## 📚 Documentation

For documentation about the **workflows** that generate this content:

### Courses
- **Workflow docs:** `expansion-packs/creator-os/docs/course/`
- **Tasks:** `expansion-packs/creator-os/tasks/`
- **Templates:** `expansion-packs/creator-os/templates/`

### Minds
- **Workflow docs:** `expansion-packs/mmos-mind-mapper/docs/`
- **Scripts:** `expansion-packs/mmos-mind-mapper/scripts/`
- **Library:** `expansion-packs/mmos-mind-mapper/lib/`

### Real Documentation
- **Architecture:** `docs/architecture/`
- **Guides:** `docs/guides/`
- **PRDs:** `docs/prd/`

---

## 🔧 Usage

### Generating Courses

```bash
# 1. Initialize course structure
*generate-course my-course-name

# 2. Fill the brief
# Edit: outputs/courses/my-course-name/COURSE-BRIEF.md

# 3. Generate content
*continue-course my-course-name

# Output: outputs/courses/my-course-name/
```

### Processing Minds

```bash
# Process a mind from source materials
*activate-clone {mind-name}

# Output: outputs/minds/{mind-name}/
```

---

## 🗑️ Cleanup

To clean generated outputs:

```bash
# Remove all generated courses
rm -rf outputs/courses/*

# Remove all processed minds
rm -rf outputs/minds/*

# Remove database (WARNING: all data will be lost!)
rm -rf outputs/database/*

# Keep directory structure
mkdir -p outputs/{courses,minds,database}
touch outputs/{courses,minds,database}/.gitkeep
```

---

## 📊 Statistics

To see what's generated:

```bash
# Count generated courses
ls -1 outputs/courses/ | wc -l

# Count processed minds
ls -1 outputs/minds/ | wc -l

# Disk usage
du -sh outputs/
```

---

**Created:** 2025-10-17
**Refactored from:** `docs/` (courses, minds)
**Rationale:** Separate generated outputs from documentation
