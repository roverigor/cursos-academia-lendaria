# Aula 2.20: Closer AI - Proposta Automática

**Módulo:** 2 - AI Sales Machine
**Nível:** 2
**Duração:** 15 min

---

## 🎯 Objetivos

1. Gerar propostas automaticamente pós-reunião
2. Personalizar com GPT-4 baseado em meeting notes
3. Calcular ROI específico do cliente
4. Enviar contrato via DocuSign
5. Fechar vendas < R$ 10k automaticamente

---

## 📊 Closer AI Flow

```
Meeting ends → Webhook → GPT-4 gera proposta → Send email →
Wait 2 days → Follow-up → Detect objection → Handle →
Send contract → Close
```

---

## 🤖 GPT-4: Proposal Generator

### Prompt

```
You are an expert sales proposal writer.

Generate a personalized proposal for:

CLIENT INFO:
- Company: {{ company }}
- Contact: {{ contact_name }}, {{ title }}
- Industry: {{ industry }}
- Company size: {{ company_size }} employees
- Current situation: {{ current_situation }}

MEETING NOTES:
{{ meeting_notes }}

PAIN POINTS DISCUSSED:
- {{ pain_point_1 }}
- {{ pain_point_2 }}
- {{ pain_point_3 }}

OUR SOLUTION:
- Product: AI Sales Machine
- What it does: Automates prospecting (1,500 leads/month), SDR outreach, meeting booking
- Implementation time: 7 days
- Price: R$ 5,000/month

THEIR CURRENT METRICS (from meeting):
- Leads/month: {{ current_leads }}
- Reuniões/month: {{ current_meetings }}
- Vendas/month: {{ current_sales }}
- Team size: {{ current_team_size }} SDRs
- Cost: R$ {{ current_monthly_cost }}

EXPECTED RESULTS:
- Leads: {{ current_leads }} → 1,500/month (+{{ increase_pct }}%)
- Meetings: {{ current_meetings }} → 45/month
- Sales: {{ current_sales }} → 9/month
- Cost: R$ {{ current_monthly_cost }} → R$ 6,000 (-R$ {{ savings }})
- ROI: +R$ {{ roi_monthly }}/month

OUTPUT (Professional proposal in Portuguese, 500-700 words):
Include:
1. Executive Summary (3-4 sentences)
2. Current Situation (their pain points)
3. Proposed Solution (how we solve it)
4. Expected Results (metrics + ROI)
5. Implementation Timeline (7 days)
6. Investment (R$ 5k/month)
7. Next Steps (sign + onboarding)

Tone: Professional but friendly, data-driven, confident.
```

### n8n Node

```json
{
  "name": "GPT-4 Generate Proposal",
  "type": "@n8n/n8n-nodes-langchain.openAi",
  "parameters": {
    "model": "gpt-4",
    "temperature": 0.5,
    "maxTokens": 1500,
    "prompt": "[PROMPT ACIMA]"
  }
}
```

---

## 📧 Proposal Email Template

```
Subject: Proposta: AI Sales Machine para {{ company }}

{{ contact_name }},

Foi ótimo conversar hoje!

Segue proposta baseada no que discutimos:

---

{{ gpt4_proposal_text }}

---

INVESTIMENTO:
R$ 5.000/mês

ROI ESPERADO:
+R$ {{ roi_monthly }}/mês

PAYBACK:
{{ payback_days }} dias

Próximos passos:
1. Revisa proposta
2. Alinha internamente (se necessário)
3. Assina contrato
4. Onboarding (7 dias)

Alguma dúvida?

Abs,
{{ sender }}

P.S: Contrato segue em anexo.
```

---

## 📄 Contract Generation

### DocuSign Integration

```json
{
  "name": "Send DocuSign Contract",
  "type": "n8n-nodes-base.httpRequest",
  "parameters": {
    "url": "https://demo.docusign.net/restapi/v2.1/accounts/{{accountId}}/envelopes",
    "method": "POST",
    "authentication": "oAuth2",
    "body": {
      "emailSubject": "Contrato - AI Sales Machine",
      "documents": [{
        "documentBase64": "{{ $json.contract_pdf_base64 }}",
        "name": "Contrato_AI_Sales_Machine.pdf",
        "fileExtension": "pdf",
        "documentId": "1"
      }],
      "recipients": {
        "signers": [{
          "email": "={{ $json.client_email }}",
          "name": "={{ $json.client_name }}",
          "recipientId": "1",
          "tabs": {
            "signHereTabs": [{
              "documentId": "1",
              "pageNumber": "3",
              "xPosition": "100",
              "yPosition": "200"
            }]
          }
        }]
      },
      "status": "sent"
    }
  }
}
```

---

## 💰 ROI Calculator (Personalizado)

### JavaScript Function

```javascript
function calculateROI(client) {
    // Current state
    const currentLeads = client.current_leads || 300;
    const currentMeetings = client.current_meetings || 6;
    const currentSales = client.current_sales || 1;
    const currentMonthlyCost = client.current_monthly_cost || 24000; // 3 SDRs
    const ticketMedio = client.ticket_medio || 5000;

    // With AI Sales Machine
    const newLeads = 1500;
    const newMeetings = 45;
    const newSales = 9;
    const newMonthlyCost = 6000; // R$ 2k AI + R$ 4k (1 SDR)

    // Revenue impact
    const currentRevenue = currentSales * ticketMedio;
    const newRevenue = newSales * ticketMedio;
    const revenueIncrease = newRevenue - currentRevenue;

    // Cost impact
    const costSavings = currentMonthlyCost - newMonthlyCost;

    // Total ROI
    const totalROI = revenueIncrease + costSavings;
    const roiPercentage = (totalROI / newMonthlyCost) * 100;
    const paybackDays = Math.ceil((newMonthlyCost / totalROI) * 30);

    return {
        currentRevenue,
        newRevenue,
        revenueIncrease,
        costSavings,
        totalROI,
        roiPercentage: Math.round(roiPercentage),
        paybackDays,
        summary: `Receita: +R$ ${revenueIncrease.toLocaleString()}/mês\nEconomia: +R$ ${costSavings.toLocaleString()}/mês\nROI Total: +R$ ${totalROI.toLocaleString()}/mês (${Math.round(roiPercentage)}%)\nPayback: ${paybackDays} dias`
    };
}
```

---

## 🔄 Follow-up Após Proposta

### Day 2: Check-in

```
{{ name }},

Conseguiu revisar a proposta que mandei?

Alguma dúvida ou ponto que quer discutir?

Posso te ligar em 10 min se preferir.

Abs,
{{ sender }}
```

### Day 5: Urgency (se aplicável)

```
{{ name }},

Nossa oferta de onboarding grátis (R$ 2k) acaba sexta.

Se fechar até lá, incluo:
✅ Setup completo grátis
✅ 2 horas de consultoria
✅ Templates prontos

Quer seguir?

Abs,
{{ sender }}
```

### Day 7: Final Push

```
{{ name }},

Última tentativa antes de fechar esta oportunidade.

Se não for o momento, tudo bem - podemos retomar em 3 meses.

Mas se quiser aproveitar ROI de R$ {{ roi }}/mês, preciso saber até hoje.

Responde "SIM" e te mando contrato.

Abs,
{{ sender }}
```

---

## 🎯 Quando Escalar para Humano

```javascript
const shouldEscalateToHuman = (deal) => {
    // Deal value alto
    if (deal.value >= 10000) return true;

    // Cliente enterprise (> 500 pessoas)
    if (deal.company_size > 500) return true;

    // Múltiplos stakeholders
    if (deal.stakeholders_count > 3) return true;

    // Customização complexa
    if (deal.custom_requirements) return true;

    // Objeção complexa
    if (deal.objection_type === 'COMPLEX') return true;

    return false; // AI can handle
};
```

---

## ✅ Checklist

- [ ] GPT-4 proposal generator configurado
- [ ] ROI calculator personalizado por cliente
- [ ] DocuSign integration (enviar contrato)
- [ ] Follow-up sequence (2, 5, 7 dias)
- [ ] Escalação para humano (deals > R$ 10k)
- [ ] Tracking close rate (objetivo: 50% proposal→close)

---

**🎓 Conceito:** AI Closer funciona para vendas simples (< R$ 10k, 1-2 stakeholders). Vendas complexas = humano sempre.
