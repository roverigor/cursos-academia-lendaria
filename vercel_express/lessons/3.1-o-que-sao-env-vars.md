# Aula 3.1: O Que São Env Vars (e por que existem)

**Módulo:** 3 - Environment Variables
**Duração:** 5 minutos
**Tipo:** Conceitual

---

## Objetivo da Aula

Ao final desta aula, o aluno vai:
- Entender o conceito de environment variables
- Saber quando usar env vars
- Diferenciar .env local vs Vercel env vars

---

## Roteiro

### ABERTURA (15 segundos)

```
[TELA: Título + ícone de cadeado]
```

**NARRAÇÃO:**

> Environment variables. Env vars. Variáveis de ambiente.
>
> É o erro #1 que iniciantes cometem na Vercel. Vamos resolver isso de vez.

---

### BLOCO 1: O PROBLEMA (1.5 minutos)

```
[TELA: Código com API key hardcoded]
```

**NARRAÇÃO:**

> Imagina que você tá fazendo um app que usa a API do Google Maps. Você precisa de uma API key.
>
> Opção errada:

```javascript
// NÃO FAÇA ISSO!
const apiKey = "AIzaSyD9k3j5h6g7f8d9s0a1b2c3d4e5f6g7h8i9";
```

> Você coloca a key direto no código, faz commit, push...
>
> Parabéns, você acabou de publicar sua senha no GitHub pra todo mundo ver. 🎉
>
> Bots varrem o GitHub 24/7 procurando exatamente isso. Em minutos sua key pode ser usada por outra pessoa e você recebe uma conta de milhares de reais.

```
[TELA: Meme de bill surpresa]
```

> Isso acontece. Muito.

---

### BLOCO 2: A SOLUÇÃO - ENV VARS (1.5 minutos)

```
[TELA: Animação de cofre]
```

**NARRAÇÃO:**

> Environment variables são a solução.
>
> **Analogia:** Pensa num cofre de senhas.
>
> Em vez de escrever a senha num post-it colado no monitor, você guarda num cofre. Quando precisa, vai lá e pega.
>
> No código, em vez de:

```javascript
// ❌ Errado
const apiKey = "minha-key-secreta";
```

> Você faz:

```javascript
// ✅ Certo
const apiKey = process.env.GOOGLE_MAPS_API_KEY;
```

> O código não tem a senha. A senha fica guardada separadamente, no "cofre" das env vars.

```
[TELA: Diagrama código + cofre]
```

> Você pode commitar seu código tranquilo porque a senha não tá lá.

---

### BLOCO 3: .ENV LOCAL VS VERCEL (1.5 minutos)

```
[TELA: Dois cofres - local e nuvem]
```

**NARRAÇÃO:**

> Mas onde fica esse cofre?
>
> **No seu computador:** arquivo `.env` na raiz do projeto

```
[TELA: Estrutura de pastas com .env]
```

```
meu-projeto/
├── .env         ← cofre local
├── .env.local   ← também funciona
├── package.json
└── ...
```

> Esse arquivo tem suas variáveis:

```
GOOGLE_MAPS_API_KEY=sua-key-aqui
DATABASE_URL=postgres://...
```

> E tá no `.gitignore` pra NUNCA ir pro GitHub.
>
> **Na Vercel:** você configura no dashboard

```
[TELA: Dashboard da Vercel com env vars]
```

> É outro cofre, separado. As variáveis que você coloca lá só existem na Vercel, não no seu computador.
>
> Por isso você precisa de DOIS lugares:
> - `.env` pro desenvolvimento local
> - Vercel Dashboard pra produção

---

### BLOCO 4: QUANDO USAR (30 segundos)

```
[TELA: Lista de quando usar]
```

**NARRAÇÃO:**

> Use environment variables pra:
>
> ✅ API keys (Google, Stripe, OpenAI...)
> ✅ URLs de banco de dados
> ✅ Senhas e tokens
> ✅ Configurações que mudam entre dev e produção
>
> Não precisa usar pra:
>
> ❌ Coisas públicas que não são secretas
> ❌ Constantes que nunca mudam

---

### FECHAMENTO (15 segundos)

```
[TELA: Resumo visual]
```

**NARRAÇÃO:**

> Resumindo:
>
> - Env vars = cofre de senhas do seu projeto
> - Local: arquivo `.env`
> - Produção: dashboard da Vercel
> - Nunca commit senhas no código!
>
> Na próxima aula, a parte mais confusa: client vs server. Bora!

---

## Recursos Visuais Necessários

1. Código com API key hardcoded (X vermelho)
2. Animação de cofre abrindo
3. Diagrama código + cofre separados
4. Estrutura de pastas com .env
5. Dashboard da Vercel com env vars
6. Lista de quando usar/não usar

---

## Pontos-Chave para Memorização

- Env var = cofre de senhas
- Nunca colocar secrets no código
- Local: arquivo `.env`
- Produção: dashboard Vercel
- `.env` deve estar no `.gitignore`
