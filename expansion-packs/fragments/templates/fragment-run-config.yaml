# Fragment Run Configuration Template
# Copyand fill this file before running `prepare-run`

mind_id: "00000000-0000-0000-0000-000000000000"   # Required UUID
source_id: "00000000-0000-0000-0000-000000000000" # Required UUID

document:
  path: "path/to/source.txt"                     # Absolute path to transcript/article
  document_type: "interview"                     # interview|podcast|article|video|notes|other
  language: "auto"                               # ISO 639-1 or "auto"
  structural_format_hint: "unknown"              # interview_format|monologue_format|dialogue_format|group_discussion_format|unknown

batch:
  ingestion_batch_id: null                        # Optional UUID
  generation_execution_id: null                   # Optional UUID

taxonomy:
  category_lookup_path: "templates/data/taxonomy/category_lookup.json"
  taxonomy_version: "5.0"
